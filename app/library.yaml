---
title: listener
version: 1.0.0
description: "This library starts a listener updator webapp connected with external storage and elasticsearch."
icon: python

tags:
  - training

command: python3 updator.py

requirements:
  cpu: 3.5
  memory: 7.5
  gpu: 0
  hpu: 0

image: python:3.8.10

language: python3

arguments:
  - key: data
    type: 'discrete'
    values:
      - 'data.csv'
  - key: epochs
    type: 'discrete'
    values:
      - '200'
  